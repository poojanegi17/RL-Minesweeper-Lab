# Copy this file to .env for local development (already the default if you
# just cloned the repo and run the backend locally on its default port).
#
# In production (e.g. Vercel), set VITE_API_URL as a project environment
# variable to your deployed backend's base URL instead of committing it here.
VITE_API_URL=http://localhost:8000
